export default class Airport {
  constructor(code, name, latitude, longitude, city) {
    this.code = code;
    this.name = name;
    this.latitude = latitude;
    this.longitude = longitude;
    this.city = city;
  }
}
